import React from "react";

import LoginButton from "@/components/login/LoginButton";

const home: React.FC<{}> = () => {
    return (
        <div>
            <LoginButton />
        </div>
    );
};

export default home;